for( let i = 0 ; i >= 0 ; i++ ){
    
    let userWord = prompt("Ваше слово?");
    
    if(userWord === "break"){

        break;

    } else if(userWord === "continue"){

        continue;
        
    }
    
    alert(i);
}